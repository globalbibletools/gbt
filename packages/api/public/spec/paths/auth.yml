login:
  get:
    summary: Validate magic link token
    parameters:
      - name: token
        in: query
        description: the single use token
      - name: redirectUrl
        in: query
        description: where to redirect to when login succeeds.
    responses:
      302:
        description: Redirect
  post:
    summary: Login with magic link
    requestBody:
      required: true
      content:
        application/json:
          schema:
            type: object
            properties:
              email:
                type: string
                format: email
                example: 'user@example.com'
                required: true
              redirectUrl:
                type: string
                format: url
                required: true
    responses:
      204:
        description: No Content
logout:
  post:
    summary: Log out of session
    responses:
      204:
        description: No Content
session:
  get:
    summary: Get the current session data
    responses:
      200:
        description: OK
        content:
          application/json:
            schema:
              type: object
              properties:
                user:
                  type: object
                  properties:
                    name:
                      type: string
                      example: 'Joe Translator'
                    email:
                      type: string
                      example: 'joe@example.com'
                expires:
                  type: string
                  format: date-time
