post:
  tags:
    - Language Import
  summary: Import glossing data from hebrewgreekbible.online. WARNING - this process can take some time. Requiring more than 10 minutes is normal.
  parameters:
    - $ref: '../../schemas/parameters.yml#languageCode'
  requestBody:
    required: true
    content:
      application/json:
        schema:
          type: object
          properties:
            import:
              type: string
              example: Spanish
              description: The language name (in English) on hebrewgreekbible.online.
  responses:
    204:
      description: No Content
